# Secrets — NEVER commit
.telegram
.env
.github-token
*.pem
*.key

# Claude Code local settings (user-specific, not for repo)
.claude/

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/

# Virtual envs
venv/
.venv/
env/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp
*~

# Local runtime artifacts (keep tracker logs — they're the live record)
*.log

# Ad-hoc state snapshots (rollback backups)
data/dispatch_state.baseline_*.json
data/**/*.bak_*
data/**/*.jsonl.bak_*

# Research PDFs (large, licensed content — not for repo)
research material/
.alphavantage

# External data sources (large; reproducible via scripts/fetch_*.py)
data/external/
